<Window x:Class="DDBManager.RestoreProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Restoring Files..." Height="180" Width="450" 
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize" Background="#F3F3F3">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="TxtCurrentFile" Text="Preparing..." Margin="0,0,0,5" TextTrimming="CharacterEllipsis" FontWeight="SemiBold"/>

        <ProgressBar Grid.Row="1" x:Name="PrgTotal" Height="20" Margin="0,5"/>

        <TextBlock Grid.Row="2" x:Name="TxtStats" Text="0 of 0 files restored" HorizontalAlignment="Right" Foreground="Gray" FontSize="11"/>

        <Button Grid.Row="3" Content="Cancel" HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                Width="80" Click="BtnCancel_Click" Margin="0,10,0,0"/>
    </Grid>
</Window>